export enum errorMessage {
  ipNotFound = 'IP not found',
  ipServiceNotInitialized = 'IpLocationService not initialized!',
  serviceNotReady = 'Service not ready',
  internalServerError = 'Internal server error',
}
